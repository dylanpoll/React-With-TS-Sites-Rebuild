mutation updateProject($id: Float!, $body: String!, $title: String!){
  updateProject(
    id: $id
    title: $title 
    body: $body
  ) { 
  	id 
    createdAt 
    updatedAt 
    title 
    body
  }
}